
  # CampusBite User Ordering Interface

  This is a code bundle for CampusBite User Ordering Interface. The original project is available at https://www.figma.com/design/s2rtQpUzqDzSFNA8YyhzH7/CampusBite-User-Ordering-Interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  